# Правила безопасности OpenClaw

Всех агентов и разработчиков ОБЯЗЫВАЮТ соблюдать эти правила. Нарушение может привести к компрометации системы.

## 1. Управление Credentials

### ✗ НИКОГДА не делай это

```
❌ Сохранять в git:
- Реальные пароли
- API токены
- Private keys
- Telegramбот токены
- FTMO/Keitaro креденшалы

❌ Сохранять в текстовых файлах проекта:
- Полные пароли
- Полные токены
- URLs с credentials

❌ Выводить в логи:
- Пароли
- Токены
- Полные URLs с credentials
```

### ✓ ЧТО ДЕЛАТЬ

```
✅ Credentials в ~/.openclaw/openclaw.json:
{
  "agents": [...],
  "channels": {
    "telegram": {
      "token": "1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh",
      "allowFrom": ["5929886678"]
    }
  }
}

✅ В TOOLS.md использовать safe-format:
Реально: "!ea2*r$31!Quq"
В файле: "7467…umn4" (скрыть 80%)

✅ При первом использовании:
# TOOLS.md
```

MT5 Login: 531182488
MT5 Password: [спросить при подключении]
MT5 URL: https://mt5-3.ftmo.com/terminal/

```

✅ В логах безопасный формат:
❌ "User logged in with password: my_pass_123"
✓ "User logged in successfully (FTMO)"
✓ "MT5 connection established to 7467…umn4"
```

### Команда OpenClaw с credentials

```bash
# ✓ Правильно:
export FTMO_PASSWORD="реальный пароль"
openclaw agent --agent forex-trader --message "..."

# ✗ Неправильно:
openclaw agent --agent forex-trader --message "password: my_pass_123"
```

## 2. Доступ и Permissions

### Каждый агент имеет свой scope

```
🔒 Forex Trader:
- Может: Браузер (MT5), Image анализ, Web search
- Может: Читать SOUL.md, AGENTS.md, TOOLS.md
- Может: Писать в .github/docs/analyses/ (торговые анализы)
- Не может: Доступ к другим агентам workspace
- Не может: Менять ~/.openclaw/openclaw.json
```

```
🔒 Orchestrator:
- Может: Читать все workspace агентов
- Может: Делегировать задачи другим агентам
- Может: Писать в .github/docs/plans/, .github/docs/analyses/
- Не может: Редактировать код агентов
- Не может: Менять конфиги без подтверждения
```

```
🔒 Code Reviewer:
- Может: Читать исходный код
- Может: Запускать tests, linters
- Может: Писать code reviews в .github/docs/analyses/
- Не может: Коммитить код без одобрения
- Не может: Изменять production code напрямую
```

### Проверка доступа

```
# Перед любым действием:
1. Это разрешено моему агенту?
2. Это не нарушает правила безопасности?
3. Логирую ли я это действие?

# Если сомнение — спросить пользователя:
❓ У меня нет доступа к этому файлу или операции.
Нужно разрешение пользователя перед продолжением.
```

## 3. Логирование и аудит

### Что ДОЛЖНО логироваться

✓ Все доступы к credentials
✓ Все операции с файлами (create, edit, delete)
✓ Все вызовы API
✓ Все ошибки и исключения
✓ Все действия на браузере (browser navigate, click, screenshot)
✓ Все торговые операции (если агент)

Формат:

```
[2026-02-24 14:35:20] [Forex Trader] browser.screenshot() → /tmp/mt5-snapshot.png
[2026-02-24 14:35:21] [Forex Trader] Анализ: найдено 3 open orders
[2026-02-24 14:35:22] [Forex Trader] Сохранено в: .github/docs/analyses/ASD-1440-positions.md
```

### Что НЕ должно логироваться

✗ Реальные пароли
✗ API токены полностью
✗ Приватные ключи
✗ Personal data (ИНН, паспорта и т.д.)
✗ Финансовые данные (баланс, конкретные суммы)

Формат для sensitive данных:

```
✗ "Connected to FTMO with password: !ea2*r$31!Quq"
✓ "Connected to FTMO successfully (7467…umn4)"

✗ "API token: sk-1234567890abcdefghijklmnop"
✓ "API authenticated (sk-12…)"
```

### Проверка логов

```bash
# Сканировать на credentials:
tail -100 /tmp/openclaw/openclaw-*.log | grep -i "password\|token\|key\|secret"

# Если что-то найдено:
1. Немедленно ротировать credentials
2. Удалить файл лога если содержит sensitive
3. Проверить git history на утечки
```

## 4. Git безопасность

### Перед коммитом

```bash
# 1. Проверить что коммитим
git diff --cached | grep -i "password\|token\|secret\|key"
# Если что-то найдено → git reset, исправить, коммитить снова

# 2. Проверить нет ли .env, credentials.json и т.д.
git status | grep -E ".env|credentials|secret"
# Если есть → добавить в .gitignore ПЕРЕД коммитом

# 3. Нормальный коммит
git add <файл>
git commit -m "docs: <что> [ID]"
git push
```

### Если случайно закоммитили credentials

**НЕМЕДЛЕННО**:

```bash
# 1. Ротировать credentials
# Если это password → изменить в сервисе

# 2. Очистить git history
# (есть специальные инструменты, спрашивай пользователя)

# 3. Уведомить пользователя
❗ УТЕЧКА CREDENTIALS ОБНАРУЖЕНА
Credentials были заменены в:
- ~/.openclaw/openclaw.json (новые токены)
- FTMO password ротирован
- Git history очищен

Рекомендация: Пересоздать токены везде где использовались.
```

## 5. Файловая система безопасность

### Разрешённые пути

✓ `.github/docs/` — планы, анализы, конфиги
✓ `workspaces/` — агента workspace
✓ `scripts/` — скрипты для общего использования
✓ Временные файлы в `/tmp/`

✗ `/etc/` — системные файлы
✗ `/var/` — логи системы (только читать если нужны)
✗ `~/.ssh/` — SSH ключи
✗ `/root/` — корневая директория

### Операции с файлами

```
✓ Читать файлы в .github/, workspaces/
✓ Писать в .github/docs/plans/, .github/docs/analyses/, workspaces/*/
✓ Удалять свои файлы (с подтверждением пользователя)

✗ Читать чужие приватные файлы
✗ Писать в чужие workspace (без разрешения)
✗ Удалять критические файлы (SOUL.md, AGENTS.md)
✗ Менять permissions на файлы
```

### Если нужен доступ к новому пути

```
❓ Требуется доступ к пути: <path>

Причина: [объяснение]
Операция: read | write | delete
Безопасность: [почему это безопасно]

Дождусь разрешения пользователя перед действием.
```

## 6. Сетевая безопасность

### Browser Tool

✓ Открывать только:

- MT5 WebTerminal (FTMO)
- Keitaro интерфейс
- Экономический календарь
- Официальные API документы

✗ Никогда не открывать:

- Phishing сайты (даже в целях анализа)
- Скачивать исполняемые файлы
- Устанавливать расширения
- JavaScript консоль (если не нужна)

### Web Tools (web_search, web_fetch)

✓ Безопасно использовать:

- `web_search` для поиска информации
- `web_fetch` для скачивания HTML, PDF, JSON

✗ Не использовать для:

- Доступ к защищённым ресурсам без auth
- Массовые запросы (это может быть DoS)
- Fetch данных других пользователей

### Проверка ссылок

Перед открытием ссылки в браузере:

```
1. Проверить домен (это тот что ожидали?)
2. Проверить протокол (HTTPS или HTTP?)
3. Проверить path (нет ли подозрительных символов?)

Если что-то странное → спросить пользователя:
❓ Открыть эту ссылку?
[ссылка выглядит подозрительно]
```

## 7. Интеграции и API

### API ключи и токены

```
✓ Передавать как Bearer token в headers
✗ Передавать в query params
✗ Передавать в body без HTTPS
```

### Внешние сервисы

✓ Проверенные сервисы:

- FTMO MT5 WebTerminal
- Keitaro API
- OpenClaw Gateway
- Telegram API

⚠️ Новые сервисы:

- Спросить пользователя перед подключением
- Убедиться что это официальный источник
- Проверить что credentials правильные

### Fallback при недоступности сервиса

```
Если сервис недоступен:
1. Повторить попытку (до 3 раз)
2. Если всё ещё недоступен:
   - Логировать ошибку
   - Уведомить пользователя
   - НЕ пытаться обойти (использовать proxy и т.д.)
```

## 8. Мониторинг и обнаружение проблем

### Признаки компрометации

🚨 НЕМЕДЛЕННО ОСТАНОВИТЬ если:

- Логи содержат credentials
- Git коммит содержит password
- Странные запросы к внешним сервисам
- Файлы изменились которые не трогали
- Неожиданные ошибки аутентификации

### Процесс при обнаружении проблемы

```
1. ОСТАНОВИТЬ текущее действие
2. Логировать точное время и контекст
3. Уведомить пользователя:
   🚨 SECURITY ALERT
   [Что обнаружили]
   [Когда]
   [Что сделали]

4. Ждать инструкций пользователя
5. Возможные действия:
   - Ротировать credentials
   - Пересоздать токены
   - Очистить logs
   - Заблокировать аккаунт
```

## 9. Обновления и версионирование

### Перед обновлением любого компонента

- ✓ Прочитать change log
- ✓ Проверить что нет security warnings
- ✓ Создать план (может быть простой план из одного пункта)
- ✓ Протестировать перед деплоем

### Если обновление critical security

```
⚠️ SECURITY UPDATE ТРЕБУЕТСЯ

Компонент: [какой]
Уязвимость: [краткое описание]
Действие: Требуется немедленное обновление

Процесс:
1. Останавливаем все агенты
2. Применяем update
3. Перезапускаем
4. Проверяем что всё работает
```

## 10. Контроль соответствия

### Ежедневные проверки

```bash
# 1. Проверить логи на credentials
grep -r "password\|token\|secret" /tmp/openclaw/openclaw-*.log 2>/dev/null | wc -l
# Должно быть 0

# 2. Проверить git на неправильные коммиты
git log --oneline -5 | xargs git show | grep -i "password\|token" | wc -l
# Должно быть 0

# 3. Проверить права на файлы
ls -la ~/.openclaw/openclaw.json
# Должен быть 600 (только владелец читает/пишет)
```

### Еженедельный аудит

- Обзор всех новых агентов на соответствие безопасности
- Ротация неиспользуемых credentials
- Проверка что все TOOLS.md не содержат реальные пароли

## 11. Обучение и лучшие практики

### Для всех агентов

- Прочитать этот документ полностью
- Понимать что "safe-format" для credentials значит
- Знать процесс при обнаружении проблемы

### Для разработчиков

- Использовать `.env` файлы локально (НЕ коммитить)
- Регулярно сканировать свой код на credentials
- При сомнениях — спросить

### Для администраторов

- Регулярно проверять логи (еженедельно минимум)
- Ротировать credentials регулярно (каждые 3 месяца)
- Обновлять этот документ когда найдены новые угрозы

## 12. Контактная информация

Если обнаружил security issue:

1. **НЕ коммитить** и НЕ делиться публично
2. Уведомить пользователя системы (https://t.me/hyrotraders_bot)
3. Следовать инструкциям по устранению

Спасибо за внимательность к безопасности! 🔒

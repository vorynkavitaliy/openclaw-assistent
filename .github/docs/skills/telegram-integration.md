# Skill: Telegram интеграция

## Цель

Описать как работает Telegram бот в OpenClaw, формат сообщений, маршрутизацию и доставку.

## Когда использовать

- Работа с входящими/исходящими Telegram сообщениями.
- Настройка бота, bindings, routing.
- Отладка проблем с доставкой.

## Когда НЕ использовать

- Работа с WebChat или CLI каналами.
- Внутренняя agent-to-agent коммуникация (без Telegram).

## Архитектура

```
Telegram User (@hyrotraders_bot)
      │
      ▼
OpenClaw Gateway (port 18789)
      │ plugin: @openclaw/telegram
      │
      ▼
Bindings: telegram.dm → orchestrator
      │
      ▼
Orchestrator Agent
      │ sessions_send / sessions_spawn
      ▼
Specialized Agents (forex-trader, tech-lead, ...)
```

## Конфигурация в openclaw.json

```json5
"channels": {
  "telegram": {
    "enabled": true,
    "token": "<BOT_TOKEN>",        // от @BotFather
    "dmPolicy": "allowlist",        // только разрешённые пользователи
    "allowFrom": ["tg:<USER_ID>"]   // Telegram user ID
  }
}
```

```json5
"bindings": {
  "telegram.dm": "orchestrator"     // все DM → orchestrator
}
```

## Формат сообщений

### Входящее (от пользователя)

Orchestrator получает текст сообщения напрямую. Нет специального формата — обычный текст.

### Исходящее (ответ пользователю)

Ответ Orchestrator'а автоматически доставляется в Telegram через binding.

### Формат статуса для пользователя

```
✅ Задача выполнена

**Результат**:
- Пункт 1
- Пункт 2

**Артефакты**:
- План: .github/docs/plans/<file>.md
- Анализ: .github/docs/analyses/<file>.md

**Следующие шаги**:
- Шаг 1 (если есть)
```

### Формат ошибки для пользователя

```
❌ Ошибка при выполнении

**Проблема**: <описание>
**Причина**: <что пошло не так>
**Решение**: <что нужно сделать>
```

## CLI команды для Telegram

```bash
# Проверить подключение
openclaw status --deep        # Telegram → OK

# Настроить канал
openclaw channels login --channel telegram

# Тест агента с доставкой в Telegram
openclaw agent --agent orchestrator --message "PING" --deliver

# Тест агента без доставки (только ответ в CLI)
openclaw agent --agent orchestrator --message "PING"
```

## Безопасность Telegram

- **dmPolicy: "allowlist"** — только пользователи из `allowFrom` могут писать боту.
- **Token** хранить в `~/.openclaw/openclaw.json`, не в репозитории.
- **В репо** указывать safe-format: `7467…umn4`.
- **User ID** — числовой ID Telegram пользователя (не @username).

## Типовые проблемы

### Бот не отвечает

```bash
openclaw status --deep        # Проверить Telegram статус
openclaw logs --follow        # Смотреть логи на ошибки
```

### Сообщения не доставляются

1. Проверить `bindings.telegram.dm` → должен быть `"orchestrator"`.
2. Проверить `dmPolicy` и `allowFrom`.
3. Убедиться что Gateway запущен: `openclaw status`.

### Ошибка авторизации

```bash
openclaw channels login --channel telegram
```

## Лучшие практики

- Оркестратор отвечает кратко и структурированно.
- Используй эмодзи для статусов: ✅ ❌ 🔄 ⚠️.
- Длинные ответы разбивай на несколько сообщений.
- Всегда указывай следующие шаги, если задача не завершена.

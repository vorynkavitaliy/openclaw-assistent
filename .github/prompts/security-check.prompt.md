---
name: "Проверка безопасности"
description: "Аудит кода на утечки credentials, хардкод секретов, небезопасные паттерны. Проверяет src/, skills/, workspaces/ согласно правилам .github/docs/rules/security.md."
agent: "ask"
tools: ["search", "read"]
argument-hint: "Путь для проверки (оставь пустым для проверки всего проекта)"
---

Проведи аудит безопасности: ${input:target:src/ или конкретный файл, оставь пустым для полного аудита}

## Что искать

### Критические (немедленно исправить)

- [ ] API ключи/секреты в открытом виде в коде (`apiKey = "..."`, `apiSecret = "..."`)
- [ ] Telegram Bot токен (`bot\d+:[A-Za-z0-9_-]{35,}`)
- [ ] Bybit/Binance ключи (строки похожие на `Kj8...`)
- [ ] cTrader credentials в коде
- [ ] Любой `password`, `secret`, `token`, `key` = реальное значение (не из env/config)

### Важные (исправить в ближайшем PR)

- [ ] `~/.openclaw/openclaw.json` или `credentials.json` случайно включены в .gitignore ли?
- [ ] `keys.md` в `.gitignore`?
- [ ] TOOLS.md файлы в `workspaces/` — credentials в safe-формате (`7467…umn4`)?
- [ ] `console.log` с чувствительными данными (positions, balances с деталями аккаунта)

### Паттерны которые ОК

```typescript
// ✅ Правильно — читать из конфига
const creds = getBybitCredentials(); // из ~/.openclaw/

// ✅ Правильно — safe-format в документации
// token: 531…488

// ❌ Неправильно — хардкод
const apiKey = "real-api-key-here";
```

## Формат отчёта

```
🔒 Аудит безопасности

✅ Проверено файлов: N
❌ Критических: N
⚠️  Важных: N

### Критические
- `src/...`:`строка` — [описание] → [что сделать]

### Важные
- `файл` — [описание]

### Вывод
[Статус: SAFE / NEEDS_FIXES]
```
